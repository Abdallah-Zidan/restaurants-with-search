//* default number of elements in paginated response page
export const DEFAULT_PAGE_SIZE = 10;
//* default page for paginated responses
export const DEFAULT_PAGE = 1;
//* default distance for nearby queries
export const DEFAULT_DISTANCE_IN_KM = 1;
